# Entorno
APP_ENV=production

# BD local (SQLite para autenticación de demo)
DATABASE_URL=sqlite:///./solca_parte_diario.db

# JWT (Valores de demostración, reemplazar si se requiere)
SECRET_KEY=clave_falsa_demo_123_no_usar_en_produccion_real
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# (Las variables de conexión HIS han sido removidas permanentemente)
